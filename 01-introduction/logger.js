console.log("Loading environment variable")
console.log("Makes logger available")

/**
 * 
 * --check - check syntax
 * --print - evaluate the next argument and print the result
 * --eval - evaluate the next argument
 * --require - preload the next argument and then start the process 
 * --stack-trace-limit - increase the stack trace
 * --watch - restarts the process if the entry file or anything it imports changes
 * 
 * --v8-options - get hte list of v8 config flags
 */

// nodemon
console.log("Now built in - we can remove Nodemon")